<% if flash[:alert] or (resource and resource.errors.any?) %>
  <div class="alert alert-warning fade show m-0" role="alert">
    <div class="d-none d-md-block">
      <div class="container">
        <strong>Ops!</strong>
        
        <% if resource.errors.any? %>
          <%= resource.errors.full_messages.first %>
        <% else %>
          <%= flash[:alert] %>
        <% end %>

        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    </div>

    <div class="d-block d-md-none">
      <strong>Ops!</strong>
      
      <% if resource.errors.any? %>
        <%= resource.errors.full_messages.first %>
      <% else %>
        <%= flash[:alert] %>
      <% end %>

      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  </div>
<% end %>

<% if flash[:notice] %>
  <div class="alert alert-success fade show m-0" role="alert">
    <div class="d-none d-md-block">
      <div class="container">
        <%= flash[:notice] %>

        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    </div>
    
    <div class="d-block d-md-none">
      <%= flash[:notice] %>

      <button type="button" class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  </div>
<% end %>